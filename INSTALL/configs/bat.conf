# Thème
--theme="violet-chaton"

# Style : numéros de ligne + en-tête de fichier, sans la grille
--style="numbers,header"

# Couleurs automatiques (désactivées en pipe pour éviter les codes ANSI bruts)
--color=auto
